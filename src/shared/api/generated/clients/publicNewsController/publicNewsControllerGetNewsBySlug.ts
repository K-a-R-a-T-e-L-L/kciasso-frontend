/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/shared/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/shared/api/client";
import type {
  PublicNewsControllerGetNewsBySlugQueryResponse,
  PublicNewsControllerGetNewsBySlugPathParams,
  PublicNewsControllerGetNewsBySlug404,
} from "../../types/PublicNewsControllerGetNewsBySlug";

function getPublicNewsControllerGetNewsBySlugUrl(
  slug: PublicNewsControllerGetNewsBySlugPathParams["slug"],
) {
  const res = { method: "GET", url: `/api/public/news/${slug}` as const };
  return res;
}

/**
 * @summary Get published news article by slug
 * {@link /api/public/news/:slug}
 */
export async function publicNewsControllerGetNewsBySlug(
  slug: PublicNewsControllerGetNewsBySlugPathParams["slug"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    PublicNewsControllerGetNewsBySlugQueryResponse,
    ResponseErrorConfig<PublicNewsControllerGetNewsBySlug404>,
    unknown
  >({
    method: "GET",
    url: getPublicNewsControllerGetNewsBySlugUrl(slug).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
