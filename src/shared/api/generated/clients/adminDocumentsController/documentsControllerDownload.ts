/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/shared/api/client";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/shared/api/client";
import type {
  DocumentsControllerDownloadQueryResponse,
  DocumentsControllerDownloadPathParams,
} from "../../types/DocumentsControllerDownload";

function getDocumentsControllerDownloadUrl(
  id: DocumentsControllerDownloadPathParams["id"],
  versionId: DocumentsControllerDownloadPathParams["versionId"],
) {
  const res = {
    method: "GET",
    url: `/api/admin/documents/${id}/versions/${versionId}/download` as const,
  };
  return res;
}

/**
 * @summary Download a document version for an authorized admin
 * {@link /api/admin/documents/:id/versions/:versionId/download}
 */
export async function documentsControllerDownload(
  id: DocumentsControllerDownloadPathParams["id"],
  versionId: DocumentsControllerDownloadPathParams["versionId"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    DocumentsControllerDownloadQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getDocumentsControllerDownloadUrl(id, versionId).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
