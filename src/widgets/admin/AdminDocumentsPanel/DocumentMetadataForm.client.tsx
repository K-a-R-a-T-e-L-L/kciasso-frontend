"use client";import type { FormEvent } from "react";import type { FormState } from "./types";import cls from "./DocumentMetadataForm.module.scss";
type Props={mode:"create"|"edit";form:FormState;onFieldChange:(key:keyof FormState,value:string)=>void;placements:string[];onOpenPlacement:()=>void;onSubmit:(e:FormEvent<HTMLFormElement>)=>void;busy:boolean;onCancel?:()=>void;file?:File|null;onFileChange?:(f:File|null)=>void};
export default function DocumentMetadataForm({mode,form,onFieldChange,placements,onOpenPlacement,onSubmit,busy,onCancel,file,onFileChange}:Props){const create=mode==="create";return <form className={cls.inlineForm} onSubmit={onSubmit}>{!create?<h3>Редактирование документа</h3>:null}<label>Название<input value={form.title} onChange={e=>onFieldChange("title",e.target.value)} required/></label><label>Описание<textarea value={form.description} onChange={e=>onFieldChange("description",e.target.value)} rows={3}/></label><div className={cls.grid}><label>Номер<input value={form.documentNumber} onChange={e=>onFieldChange("documentNumber",e.target.value)}/></label><label>Дата<input type="date" value={form.documentDate} onChange={e=>onFieldChange("documentDate",e.target.value)}/></label></div><button type="button" className={cls.placementSummaryButton} onClick={onOpenPlacement}>Выбрано разделов: {placements.length} · Изменить размещение</button>{create?<label>Файл<input type="file" accept=".pdf,.doc,.docx,.xls,.xlsx,.zip,.jpg,.jpeg,.png" onChange={e=>onFileChange?.(e.target.files?.[0]??null)} required/></label>:null}<div className={cls.actions}><button type="submit" className={cls.primary} disabled={busy}>{busy?(create?"Загрузка…":"Сохранение…"):(create?"Создать документ":"Сохранить")}</button>{onCancel?<button type="button" onClick={onCancel}>Отмена</button>:null}</div>{create&&file?<small>{file.name}</small>:null}</form>}
